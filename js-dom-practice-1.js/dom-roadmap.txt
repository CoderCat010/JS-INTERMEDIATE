//-----> 🎯 ১. এলিমেন্ট খুঁজে বের করা (Selecting Elements):

    1. আইডি দিয়ে খুঁজা: getElementById()

    2. ক্লাস দিয়ে খুঁজা: getElementsByClassName()

    3. ট্যাগ নাম দিয়ে খুঁজা: getElementsByTagName()

    4. CSS সিলেক্টর দিয়ে প্রথম এলিমেন্ট খুঁজা: querySelector()

    5. CSS সিলেক্টর দিয়ে সব এলিমেন্ট খুঁজা: querySelectorAll()

    6. নাম (name attribute) দিয়ে খুঁজা: getElementsByName()
